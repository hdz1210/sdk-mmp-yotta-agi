name: mmp_sdk_flutter
description: MMP SDK for Flutter - Deep Link Attribution & Deferred Deep Linking
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  app_links: ^3.4.0
  device_info_plus: ^9.1.1
  http: ^1.1.0
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
